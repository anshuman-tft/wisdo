import {Container} from "inversify";

export class ServicesDi {
    public static registerDI(container: Container, symbols: any) {
        /*container.bind<NotificationManager>(symbols.NotificationManager).to(NotificationManager);*/
    }
}